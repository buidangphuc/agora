# Runtime deps (mirrors pyproject [project.dependencies]). Pinned for the Docker
# build; the Spark BigQuery connector is a jar (recsys/spark.py), not a pip dep.
pyspark==3.5.1
numpy>=1.24
qdrant-client>=1.7
redis>=5.0
pandas>=2.0
pyarrow>=14.0
