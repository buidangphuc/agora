# CLAUDE.md

This workspace's agent guidance lives in **[AGENTS.md](AGENTS.md)** (portable
across Antigravity / Claude Code / Cursor / Codex). Read it first — it maps every
repo, the rules you must not break, and the recipe for adding features.

Architecture source of truth: `platform-core/docs/` + `platform-core/docs/ADR/`.
Don't duplicate rules here — edit the doc/ADR, then AGENTS.md.
