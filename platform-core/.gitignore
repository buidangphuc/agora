# platform-core generated code is a VERIFICATION artifact — regenerated by
# `make proto`, never edited by hand, never committed here. (Consumers commit
# their own generated code in their own repos; see ADR-0001.)
/gen/

# env / secrets
.env
*.env
!.env.example

# language junk
node_modules/
__pycache__/
*.pyc
.venv/
target/
build/
dist/
.gradle/

# os / editor
.DS_Store
