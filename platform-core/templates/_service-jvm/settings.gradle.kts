// TEMPLATE — rename this project before first commit.
//
// Pick the real service name (e.g. "listing-service") and update every place
// that reads `team-service`: this file, build.gradle.kts, the Dockerfile,
// .env.example (OTEL_SERVICE_NAME), and buf.gen.yaml if you namespace output.
rootProject.name = "team-service"
