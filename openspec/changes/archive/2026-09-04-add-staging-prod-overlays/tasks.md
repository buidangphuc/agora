# Tasks

## 1. Code — platform-gitops (env value overlays)
- [x] `envs/staging/values.yaml` (new): mirror `envs/local/values.yaml` shape — `global.registry`
      (staging registry), `global.imagePullPolicy`, `images: { team-*: <staging tag>, search-indexer:
      <tag> }`. CI-bumpable like local.
- [x] `envs/prod/values.yaml` (new): same shape with pinned prod release tags and
      `global.imagePullPolicy: IfNotPresent`.
- [x] `envs/staging/services.yaml` + `envs/prod/services.yaml` (new): per-service overlay values
      (resources.requests/limits, replicas, autoscaling min/max + targets, ingress.host) that differ
      from local — e.g. prod raises `team-gateway`/`team-frontend` replicas + HPA `maxReplicas` and sets
      real ingress hosts; gRPC services get prod-sized requests. Keep local values as the baseline.
      NOTE ON SHAPE: the flat `charts/service` schema reads `.Values.resources`/`.Values.ingress` etc.
      per render (no chart change allowed), so env-wide knobs (probe timing) live in
      `envs/<env>/services.yaml` and PER-SERVICE per-env sizing (resources/replicas/HPA/ingress host)
      lives in `envs/<env>/services/<svc>.yaml` (created for team-gateway, team-frontend, and prod
      team-domain). A shared, verbatim per-service baseline was extracted to `envs/services/<svc>.yaml`
      from each local app's inline block so identity/ports/env are not duplicated per env.

## 2. Code — platform-gitops (ArgoCD wiring)
- [x] Introduce the env dimension without duplicating every app: prefer an **ApplicationSet** with a
      list/matrix generator over `{local, staging, prod}` × services that templates each Application's
      Helm source to `$values/envs/{{env}}/values.yaml` + `$values/envs/{{env}}/services.yaml` and the
      per-service inline defaults. Document precedence.
      DONE: `argocd/apps/appset-services.yaml` — a matrix of `{staging, prod}` × the 11 chart services,
      generating `Application` `<svc>-<env>`. Precedence (later wins): chart defaults →
      `envs/<env>/values.yaml` (images+global) → `envs/services/<svc>.yaml` (baseline) →
      `envs/<env>/services.yaml` (env-wide) → `envs/<env>/services/<svc>.yaml` (per-svc per-env,
      `ignoreMissingValueFiles`) → inline `image.repo`/`pullPolicy` composed from the env registry.
- [x] Keep the existing `local` apps working: either fold `local` into the same ApplicationSet or leave
      `argocd/apps/*.yaml` as the `local` set unchanged. Verify no behavioural change for local.
      DONE: `local` is NOT generated by the ApplicationSet — the existing `argocd/apps/*.yaml` and
      `envs/local/values.yaml` are untouched. Local render proven byte-identical (section 3).
- [x] Ensure infra/raw-manifest apps (postgres, minio, backup, monitoring) are either env-scoped or
      explicitly local-only, so staging/prod don't accidentally sync local-only infra.
      DONE: the ApplicationSet covers only the 11 chart services; infra/raw apps stay local-only
      (documented in the appset header). Real staging/prod cluster + infra wiring is out of scope
      (proposal Non-goals: cluster provisioning).

## 3. Verify — render & dry-run (no service code)
- [x] `helm template` for one HTTP service (team-gateway) and one gRPC service (team-domain) against
      staging and prod: image tag resolves from the env overlay, per-env sizing applied, hardened
      probes/resources/HPA present.
      VERIFIED (rendered with the exact layered valueFiles the ApplicationSet composes):
      - team-gateway@staging → `staging-registry.example.com/team-gateway:staging`, HPA maxReplicas 6,
        ingress `api.staging.example.com`, HTTP probes present.
      - team-gateway@prod → `registry.example.com/team-gateway:1.0.0`, resources 250m/1CPU 256Mi/512Mi,
        HPA min 3/max 10, ingress `api.example.com`, prod probe timing (initialDelaySeconds 10).
      - team-domain@staging/prod (gRPC) → env tag resolves; prod replicas 2 + 200m/1CPU requests/limits;
        native gRPC readiness/liveness/startup probes present.
      - team-identity@prod (no per-svc override) renders fine via the ignore-missing path.
- [x] `helm lint` + `kubectl apply --dry-run=client` accept both env renders for both service shapes.
      VERIFIED: `helm lint charts/service` clean (0 failed); dry-run accepted every staging/prod render
      (Deployment/Service/HPA/Ingress/SA/ExternalSecret/SecretStore). ApplicationSet manifest itself:
      `kubectl apply --dry-run=client -f argocd/apps/appset-services.yaml` accepted (Argo CRDs present).
      NOTE: the ApplicationSet controller's own Go-template expansion into child Applications is
      server/controller-side (deferred, no live cluster); its effective per-service output is what was
      helm-rendered + dry-run-accepted above.
- [x] Diff `local` render before vs after this change → no change (local not regressed).
      VERIFIED: captured `helm template` for team-domain + team-gateway (local values + each app's inline)
      BEFORE and AFTER all edits → `diff` empty for both (IDENTICAL). Chart, local values, and local apps
      were not touched.

## 4. E2E (platform-e2e)
- [x] No user-facing capability change → **no `FEATURES.yaml` scenario**; deploy-runtime overlays are
      verified by the helm-render/lint/dry-run gate in section 3 (as with `harden-service-helm`).
      RECORDED here — verification is the render/lint/dry-run + local-unchanged proof above.
- [x] `make -C platform-e2e features-check` remains green (unchanged).
      VERIFIED 2026-09-04: features-check → 60/60 automated, all manifests valid. ✓

## 5. Archive
- [x] Confirm staging + prod overlays render/lint/dry-run clean for both service shapes and local is
      byte-unchanged. (Done — section 3.)
- [x] Note the dependency satisfied: builds on `harden-service-helm` chart shape (probes/resources/HPA).
- [x] Note follow-up `add-canary-deploys` (Argo Rollouts) is filed as the P2 progressive-delivery change.
      DONE: add-canary-deploys exists as its own change. NOTE (2026-09-04): the staging/prod ApplicationSet
      was corrected this session to gate on real clusters (label `envScoped=true`) so it no longer floods
      the local marketplace ns — staging/prod overlays render only for registered remote clusters.
- [x] Flag for the CI owner: extend the image-tag bump step to also bump `envs/staging` / `envs/prod`.
      FLAGGED: CI's `envs/local/values.yaml` tag-bump step must also bump `envs/staging/values.yaml`
      and `envs/prod/values.yaml` (same `images.<svc>` keys).
- [ ] Archive the change (`/opsx:archive add-staging-prod-overlays`).
