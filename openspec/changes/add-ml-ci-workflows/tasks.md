# Tasks

## 1. Code — CI Workflows
- [x] Create `platform-recsys/.github/workflows/ci.yaml` (lint, compile, env drift, host tests, offline eval).
- [x] Create `team-ai/.github/workflows/ci.yaml` (lint, env drift, tests).
- [x] Create `platform-modelserve/.github/workflows/ci.yaml` (lint, env drift, unit tests, contract conformance).

## 2. Verification
- [x] Validate OpenSpec change (`openspec validate add-ml-ci-workflows --strict`).
- [x] Validate workflow yaml syntax.
