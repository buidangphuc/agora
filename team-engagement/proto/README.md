# proto/ — vendored platform contract

Pinned copy of platform-core's proto (ADR-0001), input to `buf generate` →
`../generated/` (gitignored). team-engagement OWNS
`platform.engagement.v1.EngagementService` (favorites + views/stats).
