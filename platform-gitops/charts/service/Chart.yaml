apiVersion: v2
name: service
description: Generic chart for a marketplace service (Deployment/Service/Ingress/NetworkPolicy). Per-service config comes from the child ArgoCD Application; image tag comes from envs/local/values.yaml (CI-bumped).
type: application
version: 0.1.0
